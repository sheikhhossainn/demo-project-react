# Simple Sign up and Login page in React.

- Used React State for input fields
- Local storage is used to save the user data

# How to run this project?

- clone the repo `git clone https://github.com/sheikhhossainn/demo-project-react.git`
- open terminal in the directory you cloned the project and run `npm run dev`

